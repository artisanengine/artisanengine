Dear <%= @order.patron.first_name %>,
		
We have received payment for the following items:

<% for line_item in @line_items %>
	<%= line_item.quantity.to_s + "x " + line_item.name %>
	<%= line_item.options unless line_item.options.empty? %>
<% end %>
		
You will receive a confirmation when your items ship.
		
With thanks,
<%= @frame.name %>