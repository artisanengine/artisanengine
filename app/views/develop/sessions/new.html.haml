= content_for( :body_class ) { 'sign_in' }

#sign_in
	%h2 Sign In

	= semantic_form_for resource, as: resource_name, url: session_path( resource_name ) do |f|
		= f.hidden_field :frame_id, value: current_frame.id

		= f.inputs :email, :password
		= f.submit 'Sign In'