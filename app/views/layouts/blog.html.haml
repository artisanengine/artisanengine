= content_for( :body_class ) { 'blog' }

= content_for( :main ) do
	%h2= blog.name
	
	#blog_wrapper
		= render 'layouts/blog/sidebar'
		%article#featured.post= yield

= render template: 'layouts/visit'