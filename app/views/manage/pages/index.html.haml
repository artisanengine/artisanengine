= content_for( :heading ) { 'Pages' }

%table#pages
	%thead
		%tr
			%th Title
	%tbody
		- for page in @pages
			%tr[ page ]
				%td.title= page.title
				%td.links
					= link_to 'Edit', edit_manage_page_path( page )